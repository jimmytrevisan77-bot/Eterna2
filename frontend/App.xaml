<Application x:Class="Eterna.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="EternaViolet">#FF2E1359</Color>
            <Color x:Key="EternaMagenta">#FF5C0F4F</Color>
            <Color x:Key="EternaCrimson">#FFC0204C</Color>
            <Color x:Key="EternaGold">#FFE7C15A</Color>
            <Color x:Key="EternaDeepBlue">#FF0B0A1A</Color>
            <LinearGradientBrush x:Key="EternaBackground" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF120320" Offset="0" />
                <GradientStop Color="#FF1A0839" Offset="0.45" />
                <GradientStop Color="#FF2B0D42" Offset="1" />
            </LinearGradientBrush>
            <SolidColorBrush x:Key="EternaPanel" Color="#FF180C2C" />
            <SolidColorBrush x:Key="EternaPanelAccent" Color="#FF261347" />
            <SolidColorBrush x:Key="EternaTextPrimary" Color="#FFF8F4FF" />
            <SolidColorBrush x:Key="EternaTextSecondary" Color="#FFB8AEE6" />
            <SolidColorBrush x:Key="EternaHighlight" Color="{StaticResource EternaGold}" />
            <SolidColorBrush x:Key="EternaDanger" Color="{StaticResource EternaCrimson}" />
            <SolidColorBrush x:Key="EternaPositive" Color="#FF3FD6AC" />
            <RadialGradientBrush x:Key="EternaHalo" RadiusX="0.7" RadiusY="0.7" GradientOrigin="0.5,0.5" Center="0.5,0.5">
                <GradientStop Color="#66E7C15A" Offset="0" />
                <GradientStop Color="#002E1359" Offset="1" />
            </RadialGradientBrush>
            <Style TargetType="ScrollViewer">
                <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
                <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource EternaTextPrimary}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="Padding" Value="12 8" />
                <Setter Property="Margin" Value="4" />
                <Setter Property="Foreground" Value="{StaticResource EternaTextPrimary}" />
                <Setter Property="Background" Value="{StaticResource EternaPanelAccent}" />
                <Setter Property="BorderBrush" Value="#55E7C15A" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="18" ShadowDepth="0" Color="#55E7C15A" />
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="" Property="Opacity" Value="0.4" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF3A176A" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#FF120E2B" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
