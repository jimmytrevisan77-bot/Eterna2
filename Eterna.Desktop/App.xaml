<Application x:Class="Eterna.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="EternaNavy">#FF10172A</Color>
            <Color x:Key="EternaGold">#FFF0B03D</Color>
            <Color x:Key="EternaCrimson">#FFC83646</Color>
            <Color x:Key="EternaTeal">#FF2FA7A0</Color>
            <SolidColorBrush x:Key="EternaBackground" Color="#FF050B16" />
            <SolidColorBrush x:Key="EternaPanel" Color="#FF111D30" />
            <SolidColorBrush x:Key="EternaPanelAccent" Color="#FF172947" />
            <SolidColorBrush x:Key="EternaTextPrimary" Color="#FFE9F2FF" />
            <SolidColorBrush x:Key="EternaTextSecondary" Color="#FF9AA4BA" />
            <SolidColorBrush x:Key="EternaHighlight" Color="{StaticResource EternaGold}" />
            <SolidColorBrush x:Key="EternaDanger" Color="{StaticResource EternaCrimson}" />
            <SolidColorBrush x:Key="EternaPositive" Color="#FF30D5A5" />
            <Style TargetType="ScrollViewer">
                <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
                <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource EternaTextPrimary}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="Padding" Value="12 8" />
                <Setter Property="Margin" Value="4" />
                <Setter Property="Foreground" Value="{StaticResource EternaTextPrimary}" />
                <Setter Property="Background" Value="{StaticResource EternaPanelAccent}" />
                <Setter Property="BorderBrush" Value="{StaticResource EternaHighlight}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="" Property="Opacity" Value="0.4" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF223760" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#FF0F1A2D" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
